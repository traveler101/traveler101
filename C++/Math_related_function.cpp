#include <iostream>
#include <cmath>

int main(){
    double x = 3.99;
    double y = 4;
    double z;

    //z = std::max(x,y);
    //z = std::min(x,y);
    //z = pow(2,3); // power function in cmath
    //z = sqrt(9); // square root function 
    //z = abs(-3); // absolute function 
    //z=round(x);
    //z =ceil(x); // round up function
    //z = floor(x); // round down function

    std::cout << z <<'\n';

    return 0;
}